<%- console.log("POSTTABLE:",JSON.stringify(dados)) %>
<%- console.log(JSON.stringify(req.session)) %>
<table border="1px">
    <thead>
    <tr>
        <th>ID</th>
        <th>ID do Usuário</th>
        <th>Título</th>
        <th>Conteúdo</th>
        <th>Data da Criação</th>
    </tr>
    </thead>
    <tbody>
        <% let i; for(i=0; i<dados.length; i++) { %>
            <% if (dados [i].username==req.session.username) {%>
                    <% }%>
                <td><%= dados[i].id %></td>
                <td><%= dados[i].id_users %></td>
                <td><%= dados[i].titulo %></td>
                <td><%= dados[i].conteudo %></td>
                <td><%= dados[i].data_criacao %></td>
            </tr>
            <% } %>
    </tbody>
    <tfoot align="center">
        
            <b><%- `Qtd de registros ${i}` %></b>
       
    </tfoot>
</table>